# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Evolution API — server-only encryption key.
# Nunca usar NEXT_PUBLIC. Server-only. AES-256-GCM exige 32 bytes (base64).
# Gerar com:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
EVOLUTION_ENCRYPTION_KEY=
